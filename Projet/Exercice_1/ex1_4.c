#include <stdio.h>
#include "graphics.h"

/* 1/ Ecrire un programme qui: 
 * 	déclare 2 variables zip et zap, qui doivent etre de type entier
 * 	affecte à zip la valeur 8 et zap la valeur 4, affecte ensuite à zip la division de zip par zap
 * 	affiche la valeur de zip.
 *	Verifier que le programme marche.
 *  2/ Recommencer en affectant 14 à zip, et 3 à zap. Le programme donne-t-il toujours le bon résultat? Essayer de comprendre
 * ce qu'il se passe, et tout écrire dans son cahier.
 * */

int main( int argc, char *argv[] )
{
	/* Déclarer ses variables ici */
	
	
	/* Ecrire son programme ici */
	
	
	return 0;
}
