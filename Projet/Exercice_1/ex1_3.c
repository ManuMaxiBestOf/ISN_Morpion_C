#include <stdio.h>
#include "graphics.h"

/* Ecrire un programme qui: 
 * 	déclare 2 variables zip et zap, qui doivent etre de type entier
 * 	affecte à zip la valeur 5 et zap la valeur 4
 * 	affecte ensuite à zip la somme de zip et zap
 * 	affiche la valeur de zip, et la valeur de zap.
 * */

int main( int argc, char *argv[] )
{
	/* Déclarer ses variables ici */
	
	
	/* Ecrire son programme ici */
	
	
	return 0;
}
